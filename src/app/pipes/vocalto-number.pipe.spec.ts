import { VocaltoNumberPipe } from './vocalto-number.pipe';

describe('VocaltoNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VocaltoNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
